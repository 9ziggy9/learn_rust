// $ rustc ./main.rs && ./main
// >>> Hello, world!
fn main() {
    // Prints line, notice macro with !
    // Not sure, but I think this is because
    // there are no variadic functions in rust.
    println!("Hello, world!");
}
